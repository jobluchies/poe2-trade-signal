# secrets
.env

# fetch cache (transient)
.cache/

# python
__pycache__/
*.pyc
.venv/
venv/

# Data + generated report live on the `data` branch, NOT main — main is code-only.
# The CI (fetch.yml) commits the accumulating DB + report there each run, so these
# are ignored here to keep main clean and prevent push collisions.
data/
output/

# Local Claude Code config
.claude/
